language: python
python:
  - "3.6"
cache: pip

install: 
  - pip install coveralls
  - pip install nosexcover
  - pip install -r requirements.txt 

# command to run tests
script: nosetests --with-xcoverage --cover-package=app --cover-tests.py 

after_success: 
  - coverage report
  - coveralls